# YYC3 pnpm 配置
# 详见 docs/technical/npmrc-explanation.md

# 单仓多包场景下提升安装兼容性(部分包未正确声明 peerDeps)
shamefully-hoist=true

# 禁止安装时的 peer dependency 警告中断(严格模式可改为 true)
strict-peer-dependencies=false

# 自动安装 peer dependencies
auto-install-peers=true

# 关闭 funding 提示
fund=false
